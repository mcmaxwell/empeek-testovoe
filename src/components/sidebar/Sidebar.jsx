import './Sidebar.scss'

import React from 'react';

const Sidebar = () => (
  <div className="sidebar">
    <a href="/" className="logo">Dairy app</a>
    <span className="sidebar__text">Comment with no sense</span>
  </div>
);

export default Sidebar;
